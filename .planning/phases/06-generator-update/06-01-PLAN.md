---
phase: 06-generator-update
plan: 01
type: execute
depends_on: []
files_modified: [generator.html]
---

<objective>
Add a view= dropdown control to the generator.html config panel.

Purpose: Expose the view param so site admins can select Spielplan vs. Tabelle when generating embed code — the widget already supports both views since Phase 5.
Output: generator.html has a labelled <select id="view-input"> with dark-theme styling, positioned after the options checkboxes and before the generate button.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@generator.html

**Established patterns from prior phases:**
- Dark-theme form elements use `background:#1a1a1a; border:1px solid #333; color:#f0f0f0; border-radius:6px; padding:10px 14px; font-size:1rem; width:100%; outline:none`
- Labels use `display:block; font-size:0.8rem; color:#888; text-transform:uppercase; letter-spacing:1px; margin-bottom:6px; margin-top:20px`
- UI text in German (this is the admin tool)

**Key constraint from Phase 1:** `view` defaults to `spielplan` — existing embeds must not break. Generator must omit `&view=` param when spielplan is selected (not append `&view=spielplan`). This will be wired in plan 06-02; this plan only adds the UI control.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add view selector CSS and HTML to config panel</name>
  <files>generator.html</files>
  <action>
    1. Add a `select` CSS rule in the `<style>` block, immediately after the `input[type=number]:focus` rule:
       ```css
       select {
         background: #1a1a1a; border: 1px solid #333; color: #f0f0f0;
         border-radius: 6px; padding: 10px 14px; font-size: 1rem; width: 100%; outline: none;
         cursor: pointer; appearance: none; -webkit-appearance: none;
       }
       select:focus { border-color: #555; }
       ```
       Do NOT use a custom dropdown arrow — `appearance:none` suffices for a minimal look.

    2. In the `<body>`, after the closing `</div>` of the `.options-grid` section and before the `<div style="margin-top:24px">` that contains the generate button, insert:
       ```html
       <label>Ansicht</label>
       <select id="view-input">
         <option value="spielplan">Spielplan (Standard)</option>
         <option value="table">Tabelle</option>
       </select>
       ```
       `spielplan` must be the first option (default selected). No `selected` attribute needed since it is first.
  </action>
  <verify>Open generator.html in a browser. Confirm: "ANSICHT" label appears below the checkboxes, above the generate button; select renders with dark background and two options "Spielplan (Standard)" and "Tabelle".</verify>
  <done>Label and select visible in correct position; dark-themed matching other inputs; two options present with spielplan pre-selected.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `select` CSS rule present after `input[type=number]:focus` in `<style>` block
- [ ] `<select id="view-input">` present in HTML between `.options-grid` and generate button div
- [ ] Two `<option>` elements: `value="spielplan"` first, `value="table"` second
- [ ] No JS changes in this plan (wiring is 06-02)
</verification>

<success_criteria>

- Task completed
- View selector renders correctly in dark theme
- No regressions to existing form elements
- No embed-code logic touched yet
  </success_criteria>

<output>
After completion, create `.planning/phases/06-generator-update/06-01-SUMMARY.md`:

# Phase 6 Plan 01: Add view= dropdown to config panel — Summary

**[Substantive one-liner]**

## Accomplishments

- [What was added]

## Files Created/Modified

- `generator.html` — Description

## Decisions Made

None

## Issues Encountered

None

## Next Step

Ready for 06-02-PLAN.md
</output>
