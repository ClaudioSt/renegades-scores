---
phase: 03-league-config
plan: 01
type: execute
depends_on: []
files_modified: [league-config.json]
---

<objective>
Create `league-config.json` — the manually maintained data file that maps each league+season to its gameday IDs and promotion-restricted team IDs.

Purpose: Provide the data foundation that Phase 4 (`_gen_snapshot.js` extension) and Phase 5 (table view) depend on for computing standings and flagging blocked teams.
Output: `league-config.json` at project root with FF BL 2026 fully specified, RL Bayern 2026 and DKB DFFL 2026 as stubs.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-league-config/03-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create league-config.json</name>
  <files>league-config.json</files>
  <action>
    Create `league-config.json` at the project root with the following structure:
    - Top-level keys are league slugs: "ff-bl", "rl-bayern", "dkb-dffl"
    - Each league maps to a seasons object keyed by year string (e.g. "2026")
    - Each season entry has three required fields:
      - "name": human-readable string
      - "gameday_ids": array of integer gameday IDs from LeagueSphere for this league+season
      - "promotion_restricted": array of integer team IDs ineligible for promotion (manually verified per season — see 03-CONTEXT.md §28.3/§25.3)

    Exact data:
    - ff-bl / 2026: name="FF BL 2026", gameday_ids=[832,834,842,844,845], promotion_restricted=[254,492,505]
      (254=Munich Spatzen 4: Spatz3 in RL; 492=Erding Bulls II: Erding in RL; 505=Regensburg Phoenix III: Regen2 in RL)
    - rl-bayern / 2026: name="RL Bayern 2026", gameday_ids=[], promotion_restricted=[]  (stub — IDs TBD)
    - dkb-dffl / 2026: name="DKB DFFL 2026", gameday_ids=[], promotion_restricted=[]  (stub — IDs TBD)

    Use 2-space indentation. JSON has no comments — all rationale lives in 03-CONTEXT.md.
    Do NOT use trailing commas (invalid JSON).
  </action>
  <verify>node -e "const c=JSON.parse(require('fs').readFileSync('league-config.json','utf8'));const ff=c['ff-bl']['2026'];console.log(JSON.stringify(ff.gameday_ids)+' '+JSON.stringify(ff.promotion_restricted))"</verify>
  <done>File parses as valid JSON; ff-bl/2026.gameday_ids=[832,834,842,844,845]; ff-bl/2026.promotion_restricted=[254,492,505]; rl-bayern and dkb-dffl stubs present with empty arrays</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node -e "JSON.parse(require('fs').readFileSync('league-config.json','utf8'))"` — no SyntaxError
- [ ] All three leagues present: ff-bl, rl-bayern, dkb-dffl
- [ ] `ff-bl.2026.gameday_ids` has 5 entries: [832,834,842,844,845]
- [ ] `ff-bl.2026.promotion_restricted` has 3 entries: [254,492,505]
- [ ] `npm test` still passes (existing 111 tests unaffected)
</verification>

<success_criteria>
- league-config.json created at project root
- Valid JSON, no syntax errors
- FF BL 2026 data matches confirmed research in 03-CONTEXT.md
- RL Bayern 2026 and DKB DFFL 2026 stubs present
- No test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-league-config/03-01-SUMMARY.md`:

# Phase 3 Plan 01: league-config.json — Summary

**[Substantive one-liner]**

## Accomplishments

## Files Created/Modified

## Decisions Made

## Issues Encountered

## Next Step
Ready for 03-02-PLAN.md
</output>
