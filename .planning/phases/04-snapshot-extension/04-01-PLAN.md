---
phase: 04-snapshot-extension
plan: 01
type: execute
depends_on: []
files_modified: [_gen_snapshot.js, CLAUDE.md]
---

<objective>
Tag each gameday in `snapshot.json` with its league-config phase name so the widget and standings logic can filter gamedays by league+season.

Purpose: Phase 5 (Table View) needs to know which gamedays belong to which league. Adding an optional `phase` field to each gameday object (set from `league-config.json`) provides that linkage without breaking any existing embeds.
Output: `_gen_snapshot.js` annotates matching gamedays with `phase: "FF BL 2026"` (or whatever `cfg.name` says); CLAUDE.md schema docs updated.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
.planning/phases/04-snapshot-extension/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-league-config/03-02-SUMMARY.md
@_gen_snapshot.js
@league-config.json
@CLAUDE.md

**Tech stack available:** Node.js 20, CommonJS (`require`/`module.exports`), native `node:test`
**Established patterns:** `.matchAll()` enforced (security hook); `const`/`let` in Node scripts; 2-space indent; single quotes
**Constraining decisions:**
- [Phase 3-02]: `leagueConfig` shape is `{ [leagueKey]: { [season]: { name, gameday_ids[], promotion_restricted[] } } }`
- [Phase 3-02]: `loadLeagueConfig('./league-config.json')` is called first in the async IIFE; result stored as `leagueConfig`
- [Phase 1-01]: Existing embeds must never break — adding optional fields is safe; removing or renaming existing fields is not
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add phase tagging to _gen_snapshot.js</name>
  <files>_gen_snapshot.js</files>
  <action>
In the async IIFE, add a phase-tagging block immediately AFTER the game-log pass and BEFORE `console.log('Fetching team names…')`.

The block should:
1. Build a reverse map `gamedayPhaseMap` (plain object, numeric keys): iterate `Object.entries(leagueConfig)` for each `[leagueKey, seasons]`, then `Object.entries(seasons)` for each `[season, cfg]`, then `for (const gdId of cfg.gameday_ids)` → `gamedayPhaseMap[gdId] = cfg.name;`
2. Annotate: `for (const gd of withGames) { const phase = gamedayPhaseMap[gd.id]; if (phase) gd.phase = phase; }`
3. Log result: count annotated gamedays and log `'  ' + tagged + ' gamedays tagged with phase names'`

Use `.matchAll()` is not needed here (no regex). Use `const`/`let`. Keep 2-space indentation.
Do NOT modify `slimGameday()` — the phase field is added as a post-processing annotation, not during slimming.
Do NOT add a `phase` field to gamedays that are not found in any `gameday_ids` list (leave them without the field — undefined is fine).
  </action>
  <verify>Run: `node _gen_snapshot.js --rebuild` (uses existing snapshot, no network). Then check: `node -e "const s=require('./snapshot.json'); [832,834,842,844,845].forEach(id => { const gd=s.gamedays.find(g=>g.id===id); console.log(id, gd && gd.phase); })"` — should print `"FF BL 2026"` for all 5 IDs.</verify>
  <done>All 5 FF BL 2026 gameday IDs (832, 834, 842, 844, 845) have `phase: "FF BL 2026"` in the rebuilt snapshot. A random non-config gameday (e.g. ID 800) has no `phase` field. `npm test` passes (231 tests, no regressions).</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md snapshot schema to document phase field</name>
  <files>CLAUDE.md</files>
  <action>
In CLAUDE.md, find the `## snapshot.json structure` code block. Add `phase?,` (with trailing comma) as a new field on the gameday object, placed after `address,` and before `games:`. Add a brief inline comment: `// only set for gamedays in league-config.json`

The resulting gameday shape should include:
```
    id, date, name, start, league_display, address,
    phase?,           // only set for gamedays in league-config.json
    games: [...]
```

Do NOT change any other part of CLAUDE.md.
  </action>
  <verify>Read CLAUDE.md and confirm the `phase?` line appears in the snapshot schema section.</verify>
  <done>CLAUDE.md snapshot schema shows `phase?` field with comment. No other content changed.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node _gen_snapshot.js --rebuild` completes without errors
- [ ] `node -e "const s=require('./snapshot.json'); const gd=s.gamedays.find(g=>g.id===832); console.assert(gd.phase==='FF BL 2026', 'phase tag missing')"` exits 0
- [ ] `npm test` passes (no regressions)
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- Phase field is optional (non-tagged gamedays unaffected)
- No existing snapshot fields renamed or removed
</success_criteria>

<output>
After completion, create `.planning/phases/04-snapshot-extension/04-01-SUMMARY.md`:

# Phase 4 Plan 01: Tag gamedays with phase name — Summary

**[One-liner: what shipped]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `_gen_snapshot.js` — Description
- `CLAUDE.md` — Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 04-02-PLAN.md
</output>
