---
phase: 04-snapshot-extension
plan: 03
type: execute
depends_on: ["04-02"]
files_modified: [_gen_snapshot.js, tests/snapshot.test.js]
---

<objective>
Wire `computeStandings` into the snapshot generator and validate the new `standings` block in the snapshot schema tests.

Purpose: Phase 4 delivers a `standings` block in `snapshot.json` that Phase 5 (Table View) consumes. This plan adds the call-site in `_gen_snapshot.js` and verifies the output shape with schema tests.
Output: `snapshot.json` gains a `standings` top-level key; `tests/snapshot.test.js` validates its structure and FF BL 2026 data.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-plan.md
.planning/phases/04-snapshot-extension/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-snapshot-extension/04-02-SUMMARY.md
@_gen_snapshot.js
@standings.js
@league-config.json
@tests/snapshot.test.js

**Tech stack available:** Node.js 20, CommonJS, native `node:test` + `node:assert/strict`
**Established patterns:** `describe()`/`it()`; `assert.ok()`, `assert.deepEqual()`, `assert.match()`; snapshot tests read real `snapshot.json` from disk; 2-space indent; single quotes
**Constraining decisions:**
- [Phase 04-02]: `computeStandings(leagueConfig, gamedays)` returns `{ [leagueKey]: { [season]: { name, rows, promotion_restricted } } }`
- [Phase 04-02]: `rows` is `[{ team_id, team_name, Sp, S, U, N, EP, GP, PD, SQ }]` sorted SQ desc
- [Phase 01-01]: Existing embeds must never break — adding a new top-level key to snapshot.json is safe
- [CLAUDE.md]: leagueConfig already loaded as first call in async IIFE
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire computeStandings into _gen_snapshot.js</name>
  <files>_gen_snapshot.js</files>
  <action>
At the top of `_gen_snapshot.js`, add `const { computeStandings } = require('./standings.js');` alongside the existing `const { loadLeagueConfig } = require('./league-config.js');` line (keep them adjacent).

In the async IIFE, immediately after the `buildTeams` section (after `const allTeams = ...`) and before the final `const snapshot = ...` line:
1. Call: `const standings = computeStandings(leagueConfig, withGames);`
2. Log: `console.log('Standings computed for ' + Object.keys(standings).length + ' league(s)');`
3. Add `standings` to the snapshot object: change `const snapshot = { generated: TODAY, teams: allTeams, gamedays: withGames };` to include `, standings`.

Do NOT change the JSON key order of `generated`, `teams`, `gamedays` — `standings` should be appended last.
  </action>
  <verify>Run `node _gen_snapshot.js --rebuild`. Confirm: (a) "Standings computed for N league(s)" appears in output, (b) `node -e "const s=require('./snapshot.json'); console.log(typeof s.standings)"` prints `object`, (c) `node -e "const s=require('./snapshot.json'); console.log(JSON.stringify(Object.keys(s.standings['ff-bl']['2026'])))"` shows `["name","rows","promotion_restricted"]`.</verify>
  <done>snapshot.json has `standings` key with FF BL 2026 league data. All other top-level keys (`generated`, `teams`, `gamedays`) unchanged. `npm test` passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add standings schema tests to tests/snapshot.test.js</name>
  <files>tests/snapshot.test.js</files>
  <action>
Append a new `describe` block to `tests/snapshot.test.js` for standings validation. Add it after the last existing `describe` block.

Tests to include:
1. `snap.standings` is an object (not undefined, not null, not an array)
2. `snap.standings['ff-bl']` exists and is an object
3. `snap.standings['ff-bl']['2026']` exists and has keys `name`, `rows`, `promotion_restricted`
4. `snap.standings['ff-bl']['2026'].name` equals `'FF BL 2026'`
5. `snap.standings['ff-bl']['2026'].rows` is an array
6. `snap.standings['ff-bl']['2026'].promotion_restricted` deep-equals `[254, 492, 505]`
7. Each row in `rows` (if any) has all required fields: `team_id` (number), `team_name` (string), `Sp` (number), `S` (number), `U` (number), `N` (number), `EP` (number), `GP` (number), `PD` (number), `SQ` (number between 0 and 1 inclusive)

Use `assert.ok(typeof snap.standings === 'object' && snap.standings !== null && !Array.isArray(snap.standings), ...)` for test 1. Use `assert.deepEqual` for test 6. Use a `for` loop over `rows` for test 7.

Do NOT touch any existing `describe` block.
  </action>
  <verify>`npm test` — all existing tests pass plus the new standings tests. Count of tests increases by 7.</verify>
  <done>New `describe('snapshot.json standings', ...)` block present with 7 passing tests. Total test count up by 7. No existing test modified.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node _gen_snapshot.js --rebuild` completes without errors
- [ ] `snapshot.json` top-level keys: `generated`, `teams`, `gamedays`, `standings`
- [ ] `npm test` passes all tests (previous 231 + standings.test.js tests + 7 new schema tests)
- [ ] `snap.standings['ff-bl']['2026'].promotion_restricted` deep-equals `[254, 492, 505]`
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- snapshot.json includes standings block with FF BL 2026 data
- No existing snapshot keys renamed or removed
- Phase 4 complete — ready for Phase 5 (Table View)
</success_criteria>

<output>
After completion, create `.planning/phases/04-snapshot-extension/04-03-SUMMARY.md`:

# Phase 4 Plan 03: Wire standings into snapshot — Summary

**[One-liner: what shipped]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `_gen_snapshot.js` — Description
- `tests/snapshot.test.js` — Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase 4 complete, ready for Phase 5 (Table View)
</output>
